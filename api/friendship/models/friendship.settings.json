{
  "kind": "collectionType",
  "collectionName": "friendships",
  "info": {
    "name": "friendship",
    "description": ""
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": false
  },
  "attributes": {
    "owner": {
      "plugin": "users-permissions",
      "model": "user",
      "via": "friendship"
    },
    "pending": {
      "plugin": "users-permissions",
      "collection": "user"
    },
    "friend": {
      "plugin": "users-permissions",
      "collection": "user",
      "via": "users"
    }
  }
}
